// Single source of truth for the site's canonical URL. Used by metadata,
// robots.txt, sitemap.xml, and structured data — update this ONE constant
// once the real domain (montegritty.com) is live and pointed at this
// deployment. Everything else follows automatically.
export const SITE_URL = "https://montegritty.vercel.app";
