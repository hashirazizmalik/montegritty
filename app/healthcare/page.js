import VerticalPage, { verticalMetadata } from '@/components/VerticalPage';

export const metadata = verticalMetadata('healthcare');
export default function Page() { return <VerticalPage slug="healthcare" />; }
