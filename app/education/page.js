import VerticalPage, { verticalMetadata } from '@/components/VerticalPage';

export const metadata = verticalMetadata('education');
export default function Page() { return <VerticalPage slug="education" />; }
