import VerticalPage, { verticalMetadata } from '@/components/VerticalPage';

export const metadata = verticalMetadata('front-desk');
export default function Page() { return <VerticalPage slug="front-desk" />; }
